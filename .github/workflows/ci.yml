name: CI Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:

  # build:
  #   runs-on: ubuntu-latest

  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     # Set up Docker Buildx (for caching and optimized builds)
  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v3

  #     # Cache Docker layers to speed up builds
  #     - name: Cache Docker layers
  #       uses: actions/cache@v4
  #       with:
  #         path: /tmp/.buildx-cache
  #         key: ${{ runner.os }}-buildx-${{ github.sha }}
  #         restore-keys: |
  #           ${{ runner.os }}-buildx-

  #     # Build Docker Image
  #     - name: Build Docker Image
  #       run: |
  #         docker build -t msd-test-app:${{ github.sha }} .

  #     - name: Save Docker Image to Tar
  #       run: docker save -o msd-test-app.tar msd-test-app:${{ github.sha }}

  #     # Upload Docker Image as Artifact
  #     - name: Upload Docker Image Artifact
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: msd-test-app-image
  #         path: msd-test-app.tar
  #         retention-days: 7

  # PR Validation Job (Runs on PRs)
  pr-validation:
    runs-on: ubuntu-latest
    needs: build  # Ensures 'build' job runs before PR validation

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Run Linting
        run: npm run lint

      - name: Run Unit Tests
        run: npm run test

      - name: Run Code Coverage
        run: npm run coverage
        
  # pr-validation:
  #   needs: [build]
  #   runs-on: ubuntu-latest
  #   steps:
  #   - name: PR Validation
  #   # Optionally, ensure 'build' job runs first if needed
  #     uses: ./.github/actions/pr-validation.yml

  # Build Job (Builds Docker Image, runs tests)
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Build Docker Image
      uses: ./.github/workflows/setup-docker.yml
      with:
        docker-image-name: msd-test-app
        commit-sha: ${{ github.sha }}

  # Versioning Job (Handles versioning strategy for the app)
  versioning:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.set_version.outputs.VERSION }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up git config
        run: |
          git config --global user.email "actions@github.com"
          git config --global user.name "GitHub Actions"

      - name: Get the current version
        run: |
          VERSION=$(git describe --tags --always)
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "Current Version: $VERSION"

      - name: Increment version for release (if applicable)
        id: set_version
        run: |
          if [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
            echo "Bumping version"
            # Incrementing the patch version
            VERSION=$(npm version patch --no-git-tag-version)
            echo "New Version: $VERSION"
            echo "VERSION=$VERSION" >> $GITHUB_ENV
            echo "VERSION=$VERSION" >> "$GITHUB_OUTPUT"
          else
            echo "Skipping version bump"
          fi

      - name: Commit updated version if version was changed
        if: github.ref == 'refs/heads/main'
        run: |
          git add package.json package-lock.json
          git commit -m "Bump version to ${{ env.VERSION }}"

      - name: Tag the commit with new version
        run: |
          git tag -a "${{ env.VERSION  }}" -m "Release version ${{ env.VERSION  }}"
          git push origin "${{ env.VERSION  }}"

  # PR Comment Job (Optional: to add checks and comments on PR)
  pr-comment:
    runs-on: ubuntu-latest
    needs: pr-validation

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Add PR comment on success
        run: |
          if [[ "${{ needs.pr-validation.result }}" == "success" ]]; then
            echo "PR validation successful!"
          else
            echo "PR validation failed. Please check the logs."
          fi

  # Docker Publish Job (Publish to DockerHub)
  docker-publish:
    runs-on: ubuntu-latest
    needs: [build,versioning] # Don't proceed to publish if build or versioning failed
    if: github.ref == 'refs/heads/main' # Deploy only if it's the main branch

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download image tarball
        uses: actions/download-artifact@v4
        with:
          name: msd-test-app-image

      - name: Load Docker image
        run: |
          docker load -i msd-test-app.tar

      - name: Log in to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Push Docker image to DockerHub
        run: |
          docker tag msd-test-app:${{ github.sha }} kapyuval/msd-test-app:${{ github.sha }}
          docker tag msd-test-app:${{ github.sha }} msd-test-app:${{ needs.versioning.outputs.version }}
          docker tag msd-test-app:${{ github.sha }} tag msd-test-app:${{ needs.versioning.outputs.version }}
          docker push kapyuval/msd-test-app:${{ github.sha }}
          docker push kapyuval/msd-test-app:${{ needs.versioning.outputs.version }}
          docker push kapyuval/msd-test-app:latest

  # Deployment Job using ArgoCD
  # deploy:
  #   runs-on: ubuntu-latest
  #   needs: docker-publish
  #   if: github.ref == 'refs/heads/main' # Only deploy from main branch

  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v2

  #     - name: Deploy to production (example)
  #       run: |
  #         echo "Deploying version ${{ env.VERSION }} to production!"
  #         # Example: Replace this with your deploy command
  #         # kubectl apply -f deployment.yaml
